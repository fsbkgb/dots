super + alt + Escape
 bspc quit

super + ctrl + q
 bspc window -c

XF86Calculator
 gmrun

super + Escape
 pkill -USR1 -x sxhkd

~button1
 bspc pointer -g focus

super + button{1-3}
  bspc pointer -g {move,resize_side,resize_corner}

super + !button{1-3}
  bspc pointer -t %i, %i

super + @button{1-3}
  bspc pointer -u


